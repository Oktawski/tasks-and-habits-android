<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TasksAndHabits.Light" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primaryDarkColor</item>
        <item name="colorSecondaryVariant">@color/primaryLightColor</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="HeaderStyle" parent="Theme.TasksAndHabits.Light">
        <item name="fontFamily">@font/roboto</item>
    </style>

    <style name="SubheadStyle" parent="Theme.TasksAndHabits.Light">
        <item name="fontFamily">@font/roboto_light</item>
    </style>

    <style name="Theme.TasksAndHabits.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.TasksAndHabits.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.TasksAndHabits.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AlertDialog.MyTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:background">@color/primaryLightColor</item>
    </style>
</resources>